const { onSuccess, onError } = require('~/helpers');
// const { isAuthenticated } = require('~/auth');

// input is the koa-router, this function should not return anything
module.exports = (router) => {
    // router

    
};

